<%- include("../partials/header"); %> 
<div class="container pt-5 mt-5">
    <section class="mb-5">
        <h1 class="display-4 text-center pt-5">Edit Comment on </h1>
    </section>
    
    
    <form action="/campgrounds/<%= campground_id %>/comments/<%= comment._id %>?_method=PUT" method="post" class="p-2 d-flex flex-column newEditForm">
        <div class="form-group">
            <textarea class="form-control" name="comment[text]"><%= comment.text %> </textarea>
        </div>
        <div class="form-group">
            <button class="btn btn-info btn-lg  btn-block form-control"value="Submit">Submit!!</button>
        </div>
    </form>

    <section class="d-flex justify-content-center">
        <a href="/campgrounds">Go Back!!</a>
    </section>
    </div>

<%- include("../partials/footer"); %>

